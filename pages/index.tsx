import { Box } from "@mui/system";
import type { NextPage } from "next";
import RegisterationForm from "../src/components/registrationForm";


const Home: NextPage = () => {
  return  (
    <RegisterationForm />
  );
};

export default Home;
